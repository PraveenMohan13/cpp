#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;
int fun(int n)
{
    if(n/10==0)
        return 1;
    return 1+fun(n/10);
}

int main() {
int n;
    cin>>n;
    cout<<"The number of digits in "<<n<<" is "<<fun(n);
    return 0;
}
